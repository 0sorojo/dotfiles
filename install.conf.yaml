- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Work

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
   -
     command: echo "You have successfully run the dotbot Config"
     stdout: true
     stderr: true
